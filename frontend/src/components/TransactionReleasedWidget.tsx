import Preloader from "./Preloader";

const TransactionReleasedWidget = (): JSX.Element => {
  return (
    <div>
      <p>Transaction Released</p>
    </div>
  );
};

export default TransactionReleasedWidget;
